<!-- res/drawable/searchview_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!-- Background color -->
    <stroke
        android:width="1dp"
    android:color="@color/search_stroke_color" /> <!-- Stroke color -->
    <corners android:radius="10dp"/>      <!-- Optional: Rounded corners -->
</shape>
